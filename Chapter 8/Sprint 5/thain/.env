# ──────────────────────────────────────────────
# Sprint 5 — Retrieval and Knowledge Base Hardening
# Replace all placeholder values with your own Azure resource details.
# In cloud mode (THAIN_AUTH_MODE=managed_identity), key-based credentials
# are not used — leave key fields blank.
# ──────────────────────────────────────────────

# Azure AI Foundry
AZURE_AI_PROJECT_ENDPOINT=https://<your-foundry-host>.services.ai.azure.com/api/projects/<your-project>
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o

# Cosmos DB
COSMOS_ENDPOINT=https://<your-cosmos-account>.documents.azure.com:443/
COSMOS_KEY=<your-cosmos-key>
COSMOS_DATABASE=db-thain
COSMOS_CONTAINER=Complaints
COSMOS_TTL_DAYS=1

# Semantic recall (Azure AI Search — complaints index)
AZURE_SEARCH_ENDPOINT=https://<your-search-service>.search.windows.net
AZURE_SEARCH_INDEX_NAME=thain-complaints
AZURE_SEARCH_API_KEY=<your-search-api-key>
AZURE_SEARCH_MODE=semantic
AZURE_SEARCH_TOP_K=3

# Embeddings
AZURE_OPENAI_EMBEDDING_ENDPOINT=https://<your-openai-host>.cognitiveservices.azure.com
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small
AZURE_OPENAI_EMBEDDING_API_KEY=<your-embedding-api-key>
AZURE_OPENAI_EMBEDDING_API_VERSION=2024-02-15-preview

# Feature flags
ENABLE_TICKETS=true
ENABLE_NOTIFICATIONS=true
ENABLE_DOCS=true
ENABLE_WRITE_APPROVALS=false

# Multi-agent behaviour
TRIAGE_MODE=deterministic
ENABLE_RECALL=true
ENABLE_KNOWLEDGE=true

# Customer context
THAIN_CUSTOMER_ID=thain-demo-v2

# Authentication mode: "local" uses az login; "managed_identity" uses Entra ID
THAIN_AUTH_MODE=local

# Azure Key Vault (required when THAIN_AUTH_MODE=managed_identity)
KEY_VAULT_URI=https://<your-keyvault-name>.vault.azure.net/

# Key Vault secret name for the embedding API key (managed identity mode)
KV_EMBEDDING_API_KEY_NAME=thain-embedding-api-key

# Safety provider: "local" uses keyword heuristics; "acs" or "auto" uses Azure AI Content Safety
SAFETY_PROVIDER=local

# Azure AI Content Safety (required when SAFETY_PROVIDER=acs or auto)
AZURE_CONTENT_SAFETY_ENDPOINT=https://<your-content-safety-host>.cognitiveservices.azure.com
AZURE_CONTENT_SAFETY_API_KEY=<your-content-safety-key>

# Key Vault secret names for safety response templates (managed identity mode)
KV_SAFETY_TEMPLATE_REFUSE_NAME=thain-safety-template-refuse
KV_SAFETY_TEMPLATE_HUMAN_ESCALATE_NAME=thain-safety-template-human-escalate

# Application Insights (leave blank to disable cloud telemetry export)
APPINSIGHTS_CONNECTION_STRING=InstrumentationKey=<key>;IngestionEndpoint=https://<region>.in.applicationinsights.azure.com/
APPINSIGHTS_SERVICE_NAME=thain-01

# ── Sprint 5 additions ──────────────────────────────────────────────────────

# Knowledge base retrieval (Azure AI Search — KB documents index)
# Run s5-01-provision-retrieval.ps1 and s5-02-seed-kb-index.ps1 before enabling
AZURE_SEARCH_DOCS_INDEX_NAME=thain-kb-v1
AZURE_SEARCH_DOCS_TOP_K=3
